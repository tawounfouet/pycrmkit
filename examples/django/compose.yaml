services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: pycrmkit_django
      POSTGRES_USER: pycrmkit
      POSTGRES_PASSWORD: pycrmkit
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pycrmkit -d pycrmkit_django"]
      interval: 2s
      timeout: 5s
      retries: 20
