# Quickstart

The `0.0.3` release contains the engineering foundation only.

```python
import pycrmkit

print(pycrmkit.__version__)
```

The CRM facade and domain entities begin in the `0.1.x` implementation line.
